name: Duplicate workflow (disabled)

# This workflow was created by mistake earlier. It's intentionally disabled
# to avoid duplicate deploys; kept in the repo so nothing referencing it breaks.
on: []

jobs:
	noop:
		if: false
		runs-on: ubuntu-latest
		steps:
			- name: Workflow intentionally disabled
				run: echo "This workflow is disabled and will not run."
